version: '3.7'

services:
  spring_service-${ENV}:
    env_file:
      - .env
    build:
      context: ./trans-back
      args:
        - ENV=${ENV}
    ports:
      - "${PORT}:9090"
#    stdin_open: true
#    command: bash
#    tty: true
#